# 엔진-분석-일지

- 작성 일시: 2026-02-18 12:35 KST
- 작업 위치: `/Users/ryan9kim/worksoc/fairy-stockfish/dev`
- 작업 브랜치: `codex/dev`
- 작업 목표: macOS(Apple Silicon)에서 `largeboards=yes`로 Fairy-Stockfish 빌드 후 Janggi 사용 가능 상태 검증

## 1. 환경 점검

### 실행 명령
```bash
git status -sb
git branch --show-current
uname -a
uname -m
```

### 확인 결과
- 브랜치: `codex/dev`
- 아키텍처: `arm64`
- 결론: `ARCH=apple-silicon` 빌드가 적합

## 2. 빌드 옵션/장기 관련 코드 확인

### 실행 명령
```bash
rg -n "largeboards|Janggi|janggi|ARCH" README.md src/Makefile src/variant.cpp -S
```

### 확인 결과
- `src/Makefile` 기본값: `largeboards = no`
- `largeboards=yes`일 때 `-DLARGEBOARDS` 컴파일 플래그 활성화
- `src/Makefile`에서 `ARCH=apple-silicon` 공식 지원
- `src/variant.cpp`에서 `janggi`, `janggitraditional`, `janggimodern`, `janggicasual` 등록 확인

## 3. 사전 설정 검증(config sanity)

### 실행 명령
```bash
cd src
make help ARCH=apple-silicon COMP=clang
```

### 확인 결과
- `arch: 'arm64'`, `neon: 'yes'` 정상 인식
- 이 단계는 기본값으로 `largeboards: 'no'` 표시 (아직 빌드 옵션을 주지 않았기 때문)

## 4. 실제 빌드

### 실행 명령
```bash
cd src
make clean
make -j"$(sysctl -n hw.ncpu)" build ARCH=apple-silicon COMP=clang largeboards=yes
```

### 핵심 로그
- `Fairy-Stockfish specific: largeboards: 'yes'`
- `CXXFLAGS`에 `-DLARGEBOARDS -DPRECOMPUTED_MAGICS` 포함
- 링크 완료: `clang++ -o stockfish ...`

### 결과
- 산출물: `/Users/ryan9kim/worksoc/fairy-stockfish/dev/src/stockfish`

## 5. 실행 스모크 테스트 (Janggi)

### 실행 명령
```bash
cd src
./stockfish <<'EOF'
uci
setoption name UCI_Variant value janggi
isready
go depth 1
quit
EOF
```

### 핵심 결과
- 배너: `Fairy-Stockfish ... LB` (`LB`는 largeboards 빌드 지표)
- `UCI_Variant` 목록에 `janggi`, `janggicasual`, `janggimodern`, `janggitraditional` 포함
- `info string variant janggi files 9 ranks 10 ...` 출력 확인
- `bestmove` 반환 확인

## 6. 바이너리 형식 확인

### 실행 명령
```bash
ls -lh src/stockfish
file src/stockfish
```

### 결과
- 크기: 약 `955K`
- 형식: `Mach-O 64-bit executable arm64`

## 7. LiGround 연동 자료 확인 기록

### 참고한 문서
- Fairy-Stockfish GUI 위키: <https://github.com/fairy-stockfish/Fairy-Stockfish/wiki/Graphical-user-interfaces>
- LiGround README(External engines): <https://github.com/ml-research/liground#external-engines>

### 메모
- Fairy-Stockfish는 GUI 위키에서 LiGround와 호환되는 엔진으로 안내됨
- LiGround README에는 수동 설치 엔진의 파일명 규칙(`fairy-stockfish`)이 명시됨
- 실제 앱 버전에 따라 메뉴명은 다를 수 있어, 가이드 문서에는 공통 절차 중심으로 정리함

## 8. 재현용 최소 명령

```bash
cd /Users/ryan9kim/worksoc/fairy-stockfish/dev/src
make clean
make -j"$(sysctl -n hw.ncpu)" build ARCH=apple-silicon COMP=clang largeboards=yes
printf 'uci\nsetoption name UCI_Variant value janggi\nisready\nquit\n' | ./stockfish
```
