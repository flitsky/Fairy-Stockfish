# 개발자-가이드

이 문서는 Fairy-Stockfish를 macOS(Apple Silicon)에서 빌드하고, 커스텀 모드인 **장기(Janggi)** 를 사용하기 위한 최소/실무 절차를 정리한 가이드입니다.  
상세 작업 로그와 의사결정 근거는 `/Users/ryan9kim/worksoc/fairy-stockfish/dev/엔진-분석-일지.md`를 참고하세요.

## 1) 엔진 소개

Fairy-Stockfish는 Stockfish 기반의 체스 변형(variant) 엔진입니다.

- 표준 체스뿐 아니라 Xiangqi/Janggi/Shogi 등 다양한 변형 지원
- UCI/UCCI/USI/CECP(XBoard) 등 프로토콜 지원
- GUI와 연동하여 사용 가능

이 저장소에서 엔진 바이너리는 기본적으로 `src/stockfish`로 빌드됩니다.

## 2) 빌드 옵션 개괄

주요 옵션만 먼저 이해하면 됩니다.

- `ARCH`: CPU 아키텍처
  - Apple Silicon 맥북: `ARCH=apple-silicon`
- `COMP`: 컴파일러 선택
  - macOS 권장: `COMP=clang`
- `largeboards`: 대형 보드/변형 지원
  - 장기 목적이면 **반드시** `largeboards=yes`
- `all`: 매우 무거운 일부 변형까지 모두 포함
  - 보통 기본값(`no`) 유지
- `nnue`: NNUE 네트워크 임베드 활성화
  - 기본은 `no` (필요 시 별도 구성)
- `debug`, `sanitize`: 디버깅/런타임 검증용
  - 성능 빌드는 기본값 유지 (`debug=no`, `sanitize=none`)

## 3) 장기(Janggi) 활성화 핵심

핵심은 딱 2가지입니다.

1. 빌드 시 `largeboards=yes` 사용
2. 실행 후 `UCI_Variant=janggi` 설정

### 권장 빌드 명령 (Apple Silicon)

```bash
cd /Users/ryan9kim/worksoc/fairy-stockfish/dev/src
make clean
make -j"$(sysctl -n hw.ncpu)" build ARCH=apple-silicon COMP=clang largeboards=yes
```

### 검증 명령

```bash
cd /Users/ryan9kim/worksoc/fairy-stockfish/dev/src
./stockfish <<'EOF'
uci
setoption name UCI_Variant value janggi
isready
go depth 1
quit
EOF
```

아래 항목이 보이면 정상입니다.

- 시작 배너에 `LB` 표시 (largeboards 빌드)
- `UCI_Variant` 옵션 목록에 `janggi` 계열 포함
- `info string variant janggi files 9 ranks 10 ...` 출력

## 4) 실무 운영 권장 흐름 (dev/release)

- `codex/dev`: 튜닝/실험/성능 검증
- `codex/release`: 안정 버전 고정

권장 절차:

1. `dev`에서 튜닝 및 테스트
2. 기준 테스트 통과 후 `release`에 병합
3. 태그/배포 바이너리는 `release` 기준으로 관리

## 5) LiGround 앱 연동 (간략)

참고 문서:

- Fairy-Stockfish GUI 위키: <https://github.com/fairy-stockfish/Fairy-Stockfish/wiki/Graphical-user-interfaces>
- LiGround README (External engines): <https://github.com/ml-research/liground#external-engines>

일반 연동 절차:

1. 빌드된 엔진 경로 확인: `/Users/ryan9kim/worksoc/fairy-stockfish/dev/src/stockfish`
2. LiGround의 엔진 설정 화면에서 외부 엔진(UCI) 추가
3. 엔진 실행 파일 경로로 위 바이너리 지정
4. 필요 시 수동 엔진 파일명 규칙을 맞춤 (`fairy-stockfish` 이름 권장; LiGround README 기준)
5. 엔진 옵션에서 `UCI_Variant=janggi` 사용 또는 앱의 variant 선택에서 Janggi 지정

주의:

- LiGround 앱 버전별로 메뉴명/엔진 등록 UX가 다를 수 있습니다.
- 등록 후 handshake(`uci`, `isready`)가 정상인지 먼저 확인하세요.

## 6) 빠른 문제 해결

- 빌드 실패(컴파일러 없음): `xcode-select --install` 실행 후 재시도
- 아키텍처 불일치: `file src/stockfish`로 `arm64` 확인
- 장기 미표시: 빌드를 `largeboards=yes`로 다시 수행

## 부록

자세한 작업 내역, 실제 실행 명령, 핵심 로그는 아래 문서를 확인하세요.

- `/Users/ryan9kim/worksoc/fairy-stockfish/dev/엔진-분석-일지.md`
